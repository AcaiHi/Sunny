#include<iostream>
using namespace std;
const int N = 105;
int n, t[N], vis[N], ans[N];
int main(){
    int t;
    cin >> t;
    
    while (t--){
        cin >> n;
        int l = 1, r = n;
        while (l <= r){
            
        }
        for (int i = 0; i < n;  ans[i] = 0,i++)
            cout << ans[i] << " ";
        cout << endl;
    }
}